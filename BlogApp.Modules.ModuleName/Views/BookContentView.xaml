<UserControl
    x:Class="BlogApp.Modules.ModuleName.Views.BookContentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:BlogApp.Modules.ModuleName.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mdxam="https://github.com/whistyun/MdXaml"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <UserControl.Resources />

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ScrollViewer>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ListBox
                    x:Name="ArticlesListBox"
                    Grid.Column="0"
                    Background="AliceBlue"
                    ItemsSource="{Binding List}"
                    SelectedItem="{Binding SelectedArticle, Mode=TwoWay}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="SelectionChanged">
                            <i:InvokeCommandAction Command="{Binding ArticleSelectedCommand}" CommandParameter="{Binding ElementName=ArticlesListBox, Path=SelectedItem}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                    <!--  添加右键菜单  -->
                    <ListBox.ContextMenu>
                        <ContextMenu>
                            <MenuItem
                                Command="{Binding MoveUpCommand}"
                                CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                Header="上移" />
                            <MenuItem
                                Command="{Binding MoveDownCommand}"
                                CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                Header="下移" />
                            <MenuItem
                                Command="{Binding EditCommand}"
                                CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                Header="修改" />
                            <Separator />
                            <MenuItem
                                Command="{Binding DeleteCommand}"
                                CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                Header="删除" />
                        </ContextMenu>
                    </ListBox.ContextMenu>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock
                                FontFamily="Noto Serif HK"
                                FontSize="18"
                                Text="{Binding Title}"
                                ToolTip="{Binding Title}" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <Button
                    Grid.Row="1"
                    Width="200"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    BorderBrush="Black"
                    BorderThickness="1"
                    Command="{Binding AddCommand}"
                    Style="{DynamicResource MaterialDesignFlatButton}"
                    ToolTip="添加文章">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="添加文章" />
                        <materialDesign:PackIcon Kind="Plus" />
                    </StackPanel>
                </Button>
                <TextBlock
                    Grid.Row="2"
                    Margin="5"
                    Text="{Binding Message}"
                    ToolTip="{Binding Message}" />
            </Grid>
        </ScrollViewer>


        <mdxam:MarkdownScrollViewer Grid.Column="1" Markdown="{Binding SelectedArticle.Text, Mode=TwoWay}" />


    </Grid>

</UserControl>
