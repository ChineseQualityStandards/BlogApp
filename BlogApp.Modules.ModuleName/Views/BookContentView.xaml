<UserControl
    x:Class="BlogApp.Modules.ModuleName.Views.BookContentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BlogApp.Modules.ModuleName.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mdxam="https://github.com/whistyun/MdXaml"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <UserControl.Resources />
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ListBox
            Grid.Column="0"
            Width="220"
            Background="AliceBlue"
            ItemsSource="{Binding List}"
            SelectedItem="{Binding SelectedChapter}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock
                        FontFamily="Noto Serif HK"
                        Text="{Binding Title}"
                        ToolTip="{Binding Title}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBox
                AcceptsReturn="True"
                AllowDrop="True"
                Text="{Binding MdText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <mdxam:MarkdownScrollViewer Grid.Column="1" Markdown="{Binding MdText, Mode=TwoWay}" />
        </Grid>

    </Grid>
</UserControl>
