<UserControl
    x:Class="BlogApp.Modules.ModuleName.Views.ArticleEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BlogApp.Modules.ModuleName.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mdxam="https://github.com/whistyun/MdXaml"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <DockPanel LastChildFill="True">
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar ClipToBounds="False" Style="{StaticResource MaterialDesignToolBar}">
                <ListBox SelectedIndex="0">
                    <ListBoxItem Content="{materialDesign:PackIcon Kind=FormatAlignLeft}" />
                    <ListBoxItem Content="{materialDesign:PackIcon Kind=FormatAlignCenter}" />
                    <ListBoxItem Content="{materialDesign:PackIcon Kind=FormatAlignRight}" />
                    <ListBoxItem Content="{materialDesign:PackIcon Kind=FormatAlignJustify}" />
                </ListBox>
                <Separator />
                <Button Content="{materialDesign:PackIcon Kind=FormatBold}" ToolTip="加粗" />
                <Button Content="{materialDesign:PackIcon Kind=FormatItalic}" ToolTip="斜体" />
                <Button Content="{materialDesign:PackIcon Kind=FormatHeaderPound}" ToolTip="标题" />
                <Button Content="{materialDesign:PackIcon Kind=FormatStrikethroughVariant}" ToolTip="删除线" />
                <Separator />
                <Button Content="{materialDesign:PackIcon Kind=FormatListBulleted}" ToolTip="无序列表" />
                <Button Content="{materialDesign:PackIcon Kind=FormatListNumbered}" ToolTip="有序列表" />
                <Button Content="{materialDesign:PackIcon Kind=FormatListChecks}" ToolTip="待办" />
                <Button Content="{materialDesign:PackIcon Kind=FormatQuoteOpen}" ToolTip="引用" />
                <materialDesign:PopupBox>
                    <materialDesign:PopupBox.ToggleContent>
                        <materialDesign:PackIcon Kind="Xml" ToolTip="代码块" />
                    </materialDesign:PopupBox.ToggleContent>
                    <ScrollViewer
                        MaxHeight="200"
                        Style="{DynamicResource MaterialDesignScrollViewer}"
                        VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <Button Content="C#" />
                            <Button Content="WPF" />
                            <Button Content="Sql" />
                        </StackPanel>
                    </ScrollViewer>

                </materialDesign:PopupBox>

                <Button Content="{materialDesign:PackIcon Kind=Table}" ToolTip="表格" />
                <Button Content="{materialDesign:PackIcon Kind=LinkVariant}" ToolTip="超链接" />
                <Separator />
                <Button
                    Background="OrangeRed"
                    Content="发布文章"
                    ToolTip="发布文章" />
            </ToolBar>
        </ToolBarTray>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBox
                AcceptsReturn="True"
                AllowDrop="True"
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                Text="{Binding MdText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Border
                Grid.Column="1"
                Padding="5"
                BorderBrush="Gray"
                BorderThickness="1">
                <mdxam:MarkdownScrollViewer Markdown="{Binding MdText, Mode=TwoWay}" />
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
