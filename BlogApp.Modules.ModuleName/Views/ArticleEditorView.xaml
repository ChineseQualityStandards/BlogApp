<UserControl
    x:Class="BlogApp.Modules.ModuleName.Views.ArticleEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BlogApp.Modules.ModuleName.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mdxam="https://github.com/whistyun/MdXaml"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <DockPanel LastChildFill="True">
        <DockPanel
            Margin="5"
            DockPanel.Dock="Top"
            LastChildFill="True">
            <Button
                Height="40"
                Margin="5"
                Command="{Binding SendCommand}"
                Content="发布文章"
                DockPanel.Dock="Right" />
            <TextBox
                materialDesign:HintAssist.Hint="请输入文章标题"
                Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                Text="{Binding Article.Title}" />
        </DockPanel>
        <TextBlock DockPanel.Dock="Bottom" Text="{Binding Message}" />
        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBox
                AcceptsReturn="True"
                AllowDrop="True"
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                Text="{Binding Article.Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Border
                Grid.Column="1"
                Padding="5"
                BorderBrush="Gray"
                BorderThickness="1">
                <mdxam:MarkdownScrollViewer Markdown="{Binding Article.Text, Mode=TwoWay}" />
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
